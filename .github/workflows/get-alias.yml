name: Get Keystore Alias
on: workflow_dispatch
jobs:
  get:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: keytool -list -v -keystore release.keystore -storepass ${{ secrets.KEYSTORE_PASSWORD }}
