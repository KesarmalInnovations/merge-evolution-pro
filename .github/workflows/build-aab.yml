name: Merge Evolution PRO - V8 Closed Testing API 36
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Auto Restore
        run: |
          if [ ! -f "package.json" ]; then
            GOOD=$(git log --all --pretty=format:%H -- package.json | head -1)
            echo "Restoring from $GOOD"
            cp .github/workflows/build-aab.yml /tmp/b.yml
            git checkout $GOOD -- . 2>/dev/null || true
            cp /tmp/b.yml .github/workflows/build-aab.yml 2>/dev/null || true
          fi
          ls -la

      - uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '21'

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Fix Capacitor Config
        run: |
          ls -la
          npm install
          if [ ! -f "capacitor.config.json" ] && [ ! -f "capacitor.config.ts" ]; then
            echo "Creating capacitor.config.json"
            npx cap init "Merge Evolution PRO" com.kesarmallinnovations.mergeevolutionpro --web-dir=www --npm-client npm
          fi
          node -e "const fs=require('fs'); let f='capacitor.config.json'; if(fs.existsSync(f)){let j=JSON.parse(fs.readFileSync(f)); j.appId=j.appId||'com.kesarmallinnovations.mergeevolutionpro'; j.appName=j.appName||'Merge Evolution PRO'; j.webDir=j.webDir||'www'; fs.writeFileSync(f, JSON.stringify(j,null,2)); console.log('Fixed:', j)}"
          cat capacitor.config.json

      - name: Build Signed AAB
        run: |
          rm -rf android
          npx cap add android
          npx cap copy android
          mkdir -p android/app
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > android/app/upload-keystore.jks
          echo "storePassword=${{ secrets.KEYSTORE_PASSWORD }}" > android/key.properties
          echo "keyPassword=${{ secrets.KEY_PASSWORD }}" >> android/key.properties
          echo "keyAlias=${{ secrets.KEY_ALIAS }}" >> android/key.properties
          echo "storeFile=upload-keystore.jks" >> android/key.properties
          cp android/key.properties android/app/key.properties
          chmod +x android/gradlew
          cd android
          ./gradlew clean bundleRelease --no-daemon

      - name: Upload SIGNED AAB
        uses: actions/upload-artifact@v4
        with:
          name: app-release-SIGNED-FINAL
          path: android/app/build/outputs/bundle/release/app-release.aab
